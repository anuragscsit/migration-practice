<%= form_with(model: desipline, local: true) do |form| %>
  <% if desipline.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(desipline.errors.count, "error") %> prohibited this desipline from being saved:</h2>

      <ul>
        <% desipline.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_area :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
